package com.apna_dukan_backend.catalog.category.service;

import org.springframework.stereotype.Service;

/**
 * CategoryCommandService - Reserved for future command operations (create, update, delete categories).
 * Currently, categories are read-only as per business requirements.
 */
@Service
public class CategoryCommandService {
    // Future implementation for category management operations
}

